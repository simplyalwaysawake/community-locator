<!DOCTYPE html>
<html class="h-full">
  <head>
    <title>Simply Always Awake Community Locator</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <% if user_signed_in? %>
    <body class="h-full">
      <div class="min-h-full" data-controller="menu">
        <nav class="border-b border-gray-200 bg-white">
          <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
            <div class="flex h-16 justify-between">
              <div class="flex">
                <div class="flex flex-shrink-0 items-center">
                    <%= image_tag "logo.jpeg", class: "h-8 w-8 rounded-full", alt: "CommunityLocator" %>
                </div>
                <div class="hidden sm:-my-px sm:ml-6 sm:flex sm:space-x-8">
                  <!-- Current: "border-indigo-500 text-gray-900", Default: "border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700" -->
                  <%=
                    link_to "Community",
                    community_path,
                    class: "border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700 inline-flex items-center border-b-2 px-1 pt-1 text-sm font-medium",
                    data: {
                      controller: "selected-nav",
                      selected_nav_controller_value: controller_name,
                      selected_nav_target: "item",
                      item_id: "community"
                    }
                  %>

                  <%=
                    link_to "Location",
                    location_path,
                    class: "border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700 inline-flex items-center border-b-2 px-1 pt-1 text-sm font-medium",
                    data: {
                      controller: "selected-nav",
                      selected_nav_controller_value: controller_name,
                      selected_nav_target: "item",
                      item_id: "location"
                    }
                  %>

                  <%=
                    link_to "Profile",
                    edit_user_registration_path,
                    class: "border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700 inline-flex items-center border-b-2 px-1 pt-1 text-sm font-medium",
                    data: {
                      controller: "selected-nav",
                      selected_nav_controller_value: controller_name,
                      selected_nav_target: "item",
                      item_id: "profile"
                    }
                  %>

                  <%=
                    link_to "Options",
                    options_path,
                    class: "border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700 inline-flex items-center border-b-2 px-1 pt-1 text-sm font-medium",
                    data: {
                      controller: "selected-nav",
                      selected_nav_controller_value: controller_name,
                      selected_nav_target: "item",
                      item_id: "options"
                    }
                  %>

                </div>
              </div>
              <div class="hidden sm:ml-6 sm:flex sm:items-center">
                <div class="ml-4 flex items-center md:ml-6">
                    <%= link_to 'Sign out', destroy_user_session_path, data: { "turbo-method": :delete }, class: "text-gray-500 hover:text-gray-800 rounded-md px-3 py-2 text-sm font-medium" %>
                </div>
              </div>

              <div class="-mr-2 flex items-center sm:hidden">
                <!-- Mobile menu button -->
                <button data-action="menu#toggleMenu menu#toggleButton" type="button" class="relative inline-flex items-center justify-center rounded-md bg-white p-2 text-gray-400 hover:bg-gray-100 hover:text-gray-500 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2" aria-controls="mobile-menu" aria-expanded="false">
                  <span class="absolute -inset-0.5"></span>
                  <span class="sr-only">Open main menu</span>
                  <!-- Menu open: "hidden", Menu closed: "block" -->
                  <svg data-menu-target="closedButton" class="block h-6 w-6" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" aria-hidden="true">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M3.75 6.75h16.5M3.75 12h16.5m-16.5 5.25h16.5" />
                  </svg>
                  <!-- Menu open: "block", Menu closed: "hidden" -->
                  <svg data-menu-target="openButton" class="hidden h-6 w-6" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" aria-hidden="true">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
                  </svg>
                </button>
              </div>
            </div>
          </div>

          <!-- Mobile menu, show/hide based on menu state. -->
          <div data-menu-target="dropdown" class="hidden sm:hidden" id="mobile-menu">
            <div class="space-y-1 pb-3 pt-2">
              <!-- Current: "border-indigo-500 bg-indigo-50 text-indigo-700", Default: "border-transparent text-gray-600 hover:border-gray-300 hover:bg-gray-50 hover:text-gray-800" -->
              <%=
                link_to "Community",
                community_path,
                class: "border-transparent text-gray-600 hover:border-gray-300 hover:bg-gray-50 hover:text-gray-800 block border-l-4 py-2 pl-3 pr-4 text-base font-medium",
                data: {
                  controller: "selected-nav",
                  selected_nav_controller_value: controller_name,
                  selected_nav_target: "item",
                  item_id: "community"
                }
              %>

              <%=
                link_to "Location",
                location_path,
                class: "border-transparent text-gray-600 hover:border-gray-300 hover:bg-gray-50 hover:text-gray-800 block border-l-4 py-2 pl-3 pr-4 text-base font-medium",
                data: {
                  controller: "selected-nav",
                  selected_nav_controller_value: controller_name,
                  selected_nav_target: "item",
                  item_id: "location"
                }
              %>

              <%=
                link_to "Profile",
                edit_user_registration_path,
                class: "border-transparent text-gray-600 hover:border-gray-300 hover:bg-gray-50 hover:text-gray-800 block border-l-4 py-2 pl-3 pr-4 text-base font-medium",
                data: {
                  controller: "selected-nav",
                  selected_nav_controller_value: controller_name,
                  selected_nav_target: "item",
                  item_id: "profile"
                }
              %>

              <%=
                link_to "Options",
                options_path,
                class: "border-transparent text-gray-600 hover:border-gray-300 hover:bg-gray-50 hover:text-gray-800 block border-l-4 py-2 pl-3 pr-4 text-base font-medium",
                data: {
                  controller: "selected-nav",
                  selected_nav_controller_value: controller_name,
                  selected_nav_target: "item",
                  item_id: "options"
                }
              %>


              <%=
                link_to 'Sign out',
                destroy_user_session_path,
                data: {"turbo-method": :delete },
                class: "border-transparent text-gray-500 hover:border-gray-300 hover:bg-gray-50 hover:text-gray-800 block border-l-4 py-2 pl-3 pr-4 text-base font-medium"
              %>
            </div>
          </div>
        </nav>

        <div class="py-10">
          <header>
            <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
              <h1 class="text-3xl font-bold leading-tight tracking-tight text-gray-900"><%= content_for(:title) %></h1>
            </div>
          </header>
          <main>
          <div class="mt-2 mb-2 mx-auto max-w-7xl py-2 sm:py-2 lg:px-8">
              <%= render "application/flash_messages" %>
            </div>
          <div class="mx-auto max-w-7xl lg:px-8">
            <div class="rounded-lg bg-white px-5 pb-8 shadow-lg sm:px-6">
              <%= yield %>
            </div>
          </div>
        </main>
        </div>
      </div>

















      
    </body>
  <% else %>
    <body class="h-full bg-gray-50">
      <div class="min-h-full">
        <div>
          <main>
            <div class="mx-auto max-w-7xl sm:px-6 lg:px-8">
              <%= yield %>
            </div>
          </main>
        </div>
      </div>
    </body>
  <% end %>
</html>

<!DOCTYPE html>
<html class="h-full bg-gray-100">
  <head>
    <title>CommunityLocator</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <% if user_signed_in? %>
    <body class="h-full">
      <div class="min-h-full">
        <nav class="bg-gray-800" data-controller="menu">
          <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
            <div class="flex h-16 items-center justify-between">
              <div class="flex items-center">
                <div class="flex-shrink-0">
                  <%= image_tag "logo.jpeg", class: "h-8 w-8 rounded-full", alt: "CommunityLocator" %>
                </div>
                <div class="hidden md:block">
                  <div class="ml-10 flex items-baseline space-x-4">
                    <!-- Current: "bg-gray-900 text-white", Default: "text-gray-300 hover:bg-gray-700 hover:text-white" -->
                    <%=
                      link_to "Community",
                      community_path,
                      class: "text-gray-300 hover:bg-gray-700 hover:text-white rounded-md px-3 py-2 text-sm font-medium",
                      data: {
                        controller: "selected-nav",
                        selected_nav_controller_value: controller_name,
                        selected_nav_target: "item",
                        item_id: "community"
                      }
                    %>

                    <%=
                      link_to "Location",
                      location_path,
                      class: "text-gray-300 hover:bg-gray-700 hover:text-white rounded-md px-3 py-2 text-sm font-medium",
                      data: {
                        controller: "selected-nav",
                        selected_nav_controller_value: controller_name,
                        selected_nav_target: "item",
                        item_id: "location"
                      }
                    %>

                    <%=
                      link_to "Profile",
                      edit_user_registration_path,
                      class: "text-gray-300 hover:bg-gray-700 hover:text-white rounded-md px-3 py-2 text-sm font-medium",
                      data: {
                        controller: "selected-nav",
                        selected_nav_controller_value: controller_name,
                        selected_nav_target: "item",
                        item_id: "profile"
                      }
                    %>
                  </div>
                </div>
              </div>
              <div class="hidden md:block">
                <div class="ml-4 flex items-center md:ml-6">
                    <%= link_to 'Sign out', destroy_user_session_path, data: { "turbo-method": :delete }, class: "text-gray-400 hover:bg-gray-700 hover:text-white rounded-md px-3 py-2 text-sm font-medium" %>
                </div>
              </div>
              <div class="-mr-2 flex md:hidden">
                <!-- Mobile menu button -->
                <button data-action="menu#toggleMenu menu#toggleButton" type="button" class="relative inline-flex items-center justify-center rounded-md bg-gray-800 p-2 text-gray-400 hover:bg-gray-700 hover:text-white" aria-controls="mobile-menu" aria-expanded="false">
                  <span class="absolute -inset-0.5"></span>
                  <span class="sr-only">Open main menu</span>
                  <!-- Menu open: "hidden", Menu closed: "block" -->
                  <svg data-menu-target="closedButton" class="block h-6 w-6" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" aria-hidden="true">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M3.75 6.75h16.5M3.75 12h16.5m-16.5 5.25h16.5" />
                  </svg>
                  <!-- Menu open: "block", Menu closed: "hidden" -->
                  <svg data-menu-target="openButton" class="hidden h-6 w-6" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" aria-hidden="true">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
                  </svg>
                </button>
              </div>
            </div>
          </div>

          <!-- Mobile menu, show/hide based on menu state. -->
          <div data-menu-target="dropdown" class="hidden md:hidden" id="mobile-menu">
            <div class="space-y-1 px-2 pb-3 pt-2 sm:px-3">
              <!-- Current: "bg-gray-900 text-white", Default: "text-gray-300 hover:bg-gray-700 hover:text-white" -->
              <%=
                link_to "Community",
                community_path,
                class: "text-gray-300 hover:bg-gray-700 hover:text-white block rounded-md px-3 py-2 text-base font-medium",
                data: {
                  controller: "selected-nav",
                  selected_nav_controller_value: controller_name,
                  selected_nav_target: "item",
                  item_id: "community"
                }
              %>

              <%=
                link_to "Location",
                location_path,
                class: "text-gray-300 hover:bg-gray-700 hover:text-white block rounded-md px-3 py-2 text-base font-medium",
                data: {
                  controller: "selected-nav",
                  selected_nav_controller_value: controller_name,
                  selected_nav_target: "item",
                  item_id: "location"
                }
              %>

              <%=
                link_to "Profile",
                edit_user_registration_path,
                class: "text-gray-300 hover:bg-gray-700 hover:text-white block rounded-md px-3 py-2 text-base font-medium",
                data: {
                  controller: "selected-nav",
                  selected_nav_controller_value: controller_name,
                  selected_nav_target: "item",
                  item_id: "profile"
                }
              %>

              <%=
                link_to 'Sign out',
                destroy_user_session_path,
                data: {"turbo-method": :delete },
                class: "text-gray-400 hover:bg-gray-700 hover:text-white block rounded-md px-3 py-2 text-base font-medium"
              %>
            </div>
          </div>
        </nav>
        <main>
          <%= render "application/flash_messages" %>
          <header class="bg-white shadow-sm">
            <div class="mx-auto max-w-7xl px-4 py-4 sm:px-6 lg:px-8">
              <h1 class="text-lg font-semibold leading-6 text-gray-900"><%= content_for(:title) %></h1>
            </div>
          </header>
          <div class="mx-auto max-w-7xl py-6 sm:px-6 lg:px-8">
            <div class="rounded-lg bg-white px-5 py-6 shadow sm:px-6">
              <%= yield %>
            </div>
          </div>
        </main>
      </div>
    </body>
  <% else %>
    <body class="h-full bg-gray-50">
      <%= render "application/flash_messages" %>
      <div class="min-h-full">
        <div>
          <main>
            <div class="mx-auto max-w-7xl sm:px-6 lg:px-8">
              <%= yield %>
            </div>
          </main>
        </div>
      </div>
    </body>
  <% end %>
</html>

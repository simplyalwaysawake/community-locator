{
  "stack": "heroku-26",
  "buildpacks": [{ "url": "heroku/ruby" }],
  "addons": ["heroku-postgresql:mini"],
  "environments": {
    "review": {
      "scripts": {
        "postdeploy": "bundle exec rake db:seed db:fixtures:load"
      }
    }
  }
}

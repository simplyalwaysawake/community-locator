{
  "buildpacks": [{ "url": "heroku/ruby" }],
  "addons": ["heroku-postgresql:mini"],
  "environments": {
    "test": {
      "addons": ["heroku-postgresql:in-dyno"],
      "scripts": {
        "test-setup": "gem install rubocop rubocop-rails",
        "test": "rubocop .; rails test"
      }
    }
  }
}
